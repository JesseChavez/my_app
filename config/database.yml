# SQL Server (2016 or higher recommended)
# Ensure to add the right port if you are
# not using the default port 1433

development:
  adapter: sqlserver
  encoding: utf8
  host: localhost
  port: 1433
  database: my_app_development
  username: dev
  password: password

test:
  adapter: sqlserver
  encoding: utf8
  host: localhost
  port: 1433
  database: my_app_test
  username: test
  password: password

production:
  adapter: sqlserver
  encoding: utf8
  host: localhost
  port: 1433
  database: my_app_production
  username:
  password:
