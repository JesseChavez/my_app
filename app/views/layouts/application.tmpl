{{define "layout"}}
  <!DOCTYPE html>
  <html>
    <head>
      <title>My App</title>

      {{block "stylesheet" .}}
      {{end}}

      {{block "javascript" .}}
      {{end}}
    </head>
    <body>
      <h1> My Layout</h1>
      {{block "content" .}}
      {{end}}
    </body>
  </html>
{{end}}
