{{define "anonymous"}}
  <!DOCTYPE html>
  <html>
    <head>
      <title>My App</title>
      <link href="{{stylesheetPath "anonymous"}}" rel="stylesheet">
      <script src="{{javascriptPath "anonymous"}}"></script>

      {{block "stylesheet" .}}
      {{end}}

      {{block "javascript" .}}
      {{end}}
    </head>
    <body>
      <h1>Login page layout</h1>
      {{block "content" .}}
      {{end}}
    </body>
  </html>
{{end}}
